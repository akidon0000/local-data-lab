name: Claude Refactoring

on:
  schedule:
    # 毎朝3時（日本時間）= UTC 18時（前日）
    - cron: '0 18 * * *'
  workflow_dispatch:
    inputs:
      prompt:
        description: 'リファクタリングのプロンプト（空欄の場合はデフォルト）'
        required: false
        default: ''

jobs:
  refactor:
    runs-on: self-hosted
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Run Claude Refactoring
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          PROMPT="${{ github.event.inputs.prompt }}"
          if [ -z "$PROMPT" ]; then
            PROMPT="このSwiftプロジェクトをレビューし、コードの品質向上のためのリファクタリングを行ってください。変更がある場合のみコミットしてください。"
          fi

          claude -p "$PROMPT" --allowedTools "Edit,Read,Glob,Grep,Write,Bash" --dangerously-skip-permissions

      - name: Check for changes
        id: changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "refactor: Claude による自動リファクタリング"
          title: "🤖 Claude による自動リファクタリング"
          body: |
            ## 概要
            Claude Code による自動リファクタリングです。

            **実行トリガー**: ${{ github.event_name == 'schedule' && 'スケジュール実行（毎朝3時）' || '手動実行' }}

            ---
            🤖 Generated with [Claude Code](https://claude.com/claude-code)
          branch: claude-refactor-${{ github.run_number }}
          delete-branch: true
